import styled from 'styled-components';

export const AddButtonWrapper = styled.div`
  position: fixed;
  bottom: 100px;
  right: 60px;
  width: fit-content;
  height: fit-content;
  z-index: 9999;
`;
